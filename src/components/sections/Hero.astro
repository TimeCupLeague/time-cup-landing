---
import Button from '../ui/Button.astro';
import Container from '../ui/Container.astro';
import Section from '../ui/Section.astro';
import { leagueInfo } from '../../content/league';
---

<Section background="primary" class="relative overflow-hidden">
  <!-- Background decoration -->
  <div
    class="absolute inset-0 bg-gradient-to-br from-primary-600 to-primary-800"
  >
  </div>
  <div class="absolute inset-0 bg-black opacity-10"></div>

  <Container class="relative z-10">
    <div class="text-center text-white">
      <h1 class="heading-1 text-white mb-6 animate-fade-in">
        {leagueInfo.name}
      </h1>

      <p class="text-large text-white mb-8 max-w-3xl mx-auto animate-slide-up">
        {leagueInfo.tagline}
      </p>

      <p class="text-lg text-white/90 mb-12 max-w-2xl mx-auto animate-slide-up">
        {leagueInfo.description}
      </p>

      <div
        class="flex flex-col sm:flex-row gap-4 justify-center items-center animate-slide-up"
      >
        <Button
          variant="secondary"
          size="lg"
          href="#inscripcion"
          class="bg-white text-primary-500 hover:bg-primary-50 border-white"
        >
          Inscríbete Ahora
        </Button>

        <Button
          variant="ghost"
          size="lg"
          href="/bases"
          class="text-white border-white hover:bg-white hover:text-primary-500"
        >
          Ver Bases
        </Button>
      </div>

      <!-- Stats -->
      <div
        class="grid grid-cols-2 md:grid-cols-4 gap-8 mt-16 pt-16 border-t border-white/20"
      >
        <div class="text-center">
          <div class="text-3xl font-bold text-white mb-2">
            {leagueInfo.seasonDuration}
          </div>
          <div class="text-white/80">Duración</div>
        </div>

        <div class="text-center">
          <div class="text-3xl font-bold text-white mb-2">
            {leagueInfo.teamSize}
          </div>
          <div class="text-white/80">Por Equipo</div>
        </div>

        <div class="text-center">
          <div class="text-3xl font-bold text-white mb-2">
            {leagueInfo.categories}
          </div>
          <div class="text-white/80">Categorías</div>
        </div>

        <div class="text-center">
          <div class="text-3xl font-bold text-white mb-2">
            {leagueInfo.totalJornadas}
          </div>
          <div class="text-white/80">Jornadas</div>
        </div>
      </div>
    </div>
  </Container>
</Section>
