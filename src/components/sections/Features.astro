---
import Card from '../ui/Card.astro';
import Container from '../ui/Container.astro';
import Section from '../ui/Section.astro';
import { leagueFeatures } from '../../content/league';
---

<Section id="caracteristicas" class="bg-gradient-to-b from-primary-800 to-secondary-600">
  <Container>
    <div class="text-center mb-16 text-white">
      <h2 class="heading-2 mb-6">¿Por qué elegir nuestra liga?</h2>
      <p class="text-large max-w-3xl mx-auto">
        Una experiencia competitiva única que combina la pasión del Cross Training con
        la innovación del formato online y presencial.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {
        leagueFeatures.map(feature => (
          <Card class="card_opacity text-center group">
            <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-200">
              {feature.icon}
            </div>

            <h3 class="heading-3 mb-4">{feature.title}</h3>

            <p class="text-gray-600 leading-relaxed">{feature.description}</p>
          </Card>
        ))
      }
    </div>
  </Container>
</Section>
